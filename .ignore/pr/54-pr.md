title:	chore: docs/businessディレクトリ新設と業務ドキュメント初期格納（#53）
state:	MERGED
author:	shiro-ino (しろいの)
labels:	
assignees:	
reviewers:	
projects:	
milestone:	
number:	54
url:	https://github.com/shiro-ino/factone-idp-viewer/pull/54
additions:	900
deletions:	0
auto-merge:	disabled
--
# 概要

業務フロー・業務要件の格納先として `docs/business/` を新設し、記述規約の整備と初期ドキュメントの格納を行う。

## 関連Issue

Closes #53

## 変更内容

- `docs/business/README.md` 新規作成（ドキュメント一覧・対象範囲・記述方針を記載）
- `docs/README.md` のディレクトリ一覧に `business/` 行を追加
- 初期ドキュメント5件を格納
  - `factone-concept.md` — FactONE PJ全体構想
  - `operation-flow.md` — 業務フロー（レーン定義とK1〜K5・R1・F1〜F4のシーケンス図）
  - `out-of-scope.md` — PoC対象外事項一覧（各見出しに簡易説明付き）
  - `data-model.md` / `entity.md` — プレースホルダ（内容は後日追記）

## 確認事項

### 共通

- [x] ブランチ名が規約に従っている（`feature/<issue番号>-<簡易説明>` / `fix/<issue番号>` / `chore/<issue番号>-<簡易説明>`）
- [x] pre-commit のチェックがすべて通過している
- [x] 影響する設計ドキュメント（`docs/` 配下）を更新した、または更新不要である

### テスト

- [ ] `uv run pytest` / `npm run test` をローカルで実行し、すべて通過している（ドキュメントのみの変更のため対象外）
- [ ] 重点テスト領域（閲覧トークン、イベント記録、DI応答パース、座標変換、異常系）に変更がある場合、対応するテストを追加・更新した（該当なし）

### API契約

- [ ] Pydanticモデルに変更がある場合、TS型を再生成し差分をコミットした（該当なし）
- [ ] 外部公開IFに影響する場合、`docs/api` のOpenAPIエクスポートを更新した（該当なし）

### アーキテクチャ

- [ ] アーキテクチャ上の意思決定を含む場合、ADR（`docs/adr/`）を作成・更新した（該当なし）

## 動作確認方法

なし（Markdownドキュメントのみ。GitHub上でMermaid図の描画を確認可能）

## 備考

`data-model.md`・`entity.md` は内容未確定のプレースホルダ。内容追記は別Issueで実施予定。

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Comments

