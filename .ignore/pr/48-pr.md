title:	feat: 閲覧ログ外部連携API（既存イベント一覧の正式契約化）（#47）
state:	MERGED
author:	shiro-ino (しろいの)
labels:	
assignees:	
reviewers:	
projects:	
milestone:	
number:	48
url:	https://github.com/shiro-ino/factone-idp-viewer/pull/48
additions:	71
deletions:	18
auto-merge:	disabled
--
# 概要

外部連携（Power Automate等）向けの閲覧ログ取得APIを正式契約として確定する。既存の `GET /api/documents/{document_id}/events`（JWT必須・検証用）を流用し、内部管理値 `view_token_id` を応答から除去する。

## 関連Issue

Closes #47

## 変更内容

- `ViewEventSummary` から `view_token_id` を除去（内部管理値の非公開方針）
- 取得方式: REST pull・文書ID指定・全件返却（`occurred_at` 昇順）。ページネーションなし（連携側が文書ID単位で取得するため件数破綻を考慮不要）
- テスト追加: 応答形状（`view_token_id` 非含有）・昇順・0件・文書不存在404
- OpenAPI（`docs/api/openapi.json`）・TS型（`viewer/src/api/schema.d.ts`）再生成
- `docs/data-model/view-events.md` の取得節を外部連携契約として更新

## 確認事項

### 共通

- [x] ブランチ名が規約に従っている（`feature/<issue番号>-<簡易説明>` / `fix/<issue番号>` / `chore/<issue番号>-<簡易説明>`）
- [x] pre-commit のチェックがすべて通過している
- [x] 影響する設計ドキュメント（`docs/` 配下）を更新した、または更新不要である

### テスト

- [x] `uv run pytest` / `npm run test` をローカルで実行し、すべて通過している（pytest 75件・Vitest 17件）
- [x] 重点テスト領域（閲覧トークン、イベント記録、DI応答パース、座標変換、異常系）に変更がある場合、対応するテストを追加・更新した

### API契約

- [x] Pydanticモデルに変更がある場合、TS型を再生成し差分をコミットした（pre-commit の差分チェック通過）
- [x] 外部公開契約（Power Automate向けIF）に影響する場合、`docs/api` のOpenAPIエクスポートを更新した

### アーキテクチャ

- [x] アーキテクチャ上の意思決定を含む場合、ADR（`docs/adr/`）を作成・更新した → 対象決定なし（取得方式の決定は要件確定であり構造変更を伴わないためADR不要）

## 動作確認方法

1. `podman compose up -d` でローカルスタック起動
2. M2Mクライアント（factone-integration）のトークン取得
3. 閲覧URL経由で `/view/{token}/pages` にアクセス後、`GET /api/documents/{document_id}/events` を実行
4. 応答が `occurred_at` 昇順の全件で、`view_token_id` を含まないことを確認

## 備考

イベント一覧APIは #27 で検証用として先行実装済のため、本PRは契約確定（応答項目の絞り込み+テスト・ドキュメント整備）が主体。

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Comments

