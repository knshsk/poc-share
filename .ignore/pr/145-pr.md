title:	chore: コードコメントと実装のずれを修正する（#144）
state:	MERGED
labels:	
assignees:	
reviewers:	
projects:	
milestone:	
number:	145
additions:	4
deletions:	4
auto-merge:	disabled
--
# 概要

docs 整備（#140）の実装照合で見つかった、コメント・docstring が実装と食い違う箇所を直す。動作変更なし。

## 関連Issue

Closes #144

## 変更内容

- `bicep/modules/api-app.bicep`: 冒頭コメント「システム割当」→「ユーザー割当（`modules/identities.bicep`）」。実装は `identity.type: 'UserAssigned'`
- `api/app/routers/view_urls.py`: `FailureRateLimiter` docstring「固定窓」→「スライディング窓（直近 window_seconds 秒の失敗回数で判定）」。実装は deque を毎回 prune する方式
- `api/app/core/config.py`: 同コメント「固定窓」→「スライディング窓」
- `docs/data-model/view-tokens.md`: 「固定窓レート制限」→「スライディング窓レート制限（直近 60 秒に 10 失敗）」

## 確認事項

### 共通

- [x] ブランチ名が規約に従っている（`chore/144-comment-implementation-mismatch`）
- [x] pre-commit のチェックがすべて通過している
- [x] 影響する設計ドキュメント（`docs/` 配下）を更新した

### テスト

- [x] `uv run pytest` / `npm run test` — コメントのみの変更。pre-commit（ruff / mypy）通過で足りる
- [x] 重点テスト領域のロジック変更なし

### API契約

- [x] Pydantic モデルの変更なし
- [x] 外部公開 IF の変更なし

### アーキテクチャ

- [x] アーキテクチャ上の意思決定を含まない

## 動作確認方法

なし（コメント・docstring・文書のみ）

---

# Files changed

## modified

- `api/app/core/config.py`
- `api/app/routers/view_urls.py`
- `bicep/modules/api-app.bicep`
- `docs/data-model/view-tokens.md`
