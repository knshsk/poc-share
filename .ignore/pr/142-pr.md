title:	docs: モジュール構成文書（API・Viewer・認証・閲覧URL）を作成し完了条件に設計文書更新を追記する（#140）
state:	MERGED
labels:	
assignees:	
reviewers:	
projects:	
milestone:	
number:	142
additions:	445
deletions:	19
auto-merge:	disabled
--
# 概要

`docs/` に上位層を追加する作業の 2 本目。L2（モジュール構成）4 文書を新設し、L1 からのリンクを確定し、運用ルールを `CLAUDE.md` に追記する。

前提: #141（読み順案内・L1・Azure・文体統一）はマージ済み。本ブランチは `main` に rebase 済み。

## 関連Issue

Closes #140

## 変更内容

- `docs/architecture/api.md`（レイヤ構成・Protocol と実装の対応・リソース群・IDP 実行 / 文書登録 / 人手修正 / ページ画像の流れ・設定一覧）
- `docs/architecture/auth.md`（認証経路 3 種・SPA ログインフロー・JWT 検証・テナント解決の現状・realm 構成・シークレット投入）
- `docs/architecture/view-url.md`（トークンのライフサイクル・公開閲覧と閲覧イベントのシーケンス・イベント提供・防御）
- `docs/architecture/viewer.md`（画面とコンポーネント構成・ストアの責務・文書表示 / IDP 実行と修正 / 相互ハイライト / 閲覧 URL / 公開閲覧の流れ）
- `docs/architecture/README.md`・`overview.md`・`azure/README.md` の「作成予定」を実リンクに置換
- `docs/api/README.md` のテナント固有の外部システム名を「外部連携システム」に置換
- `CLAUDE.md` 完了条件に「構成・処理フロー・外部依存が変わる変更は `docs/architecture/`・`docs/azure/` を同 PR で更新する」を追記

## 既存 L3 との矛盾修正

- `docs/develop-guide/auth.md`: 動作確認手順に存在しない「API呼出」ボタンと `/api/v1/me` 表示の記述があったため、実際の遷移（文書閲覧画面の表示）に修正。API 側検証項目に RS256 限定・`sub` 必須を追記。Vite プロキシ対象に `/view/{token}/pages|pdf` を追記。`apply-keycloak-secrets.sh` が要求する管理者変数を追記
- `keycloak/README.md`: 「管理コンソールでの手動変更は永続化しない」は誤り（DB には残る）。「JSON に反映されず realm 再作成時に失われる」に修正

コード側の記述ずれ（本 PR では触れていない）:
- `api/app/routers/view_urls.py` の `FailureRateLimiter` docstring と `core/config.py` のコメントは「固定窓」だが、実装はスライディング窓
- `bicep/modules/api-app.bicep` 冒頭コメントの「システム割当」は誤り（#141 の備考と同じ）

## 確認事項

### 共通

- [x] ブランチ名が規約に従っている（`chore/140-docs-architecture-2`）
- [x] pre-commit のチェックがすべて通過している
- [x] 影響する設計ドキュメント（`docs/` 配下）を更新した

### テスト

- [x] `uv run pytest` / `npm run test` — Markdown と `CLAUDE.md` のみの変更のため対象外
- [x] 重点テスト領域の変更なし

### API契約

- [x] Pydantic モデルの変更なし
- [x] 外部公開 IF の変更なし

### アーキテクチャ

- [x] アーキテクチャ上の意思決定を含まない（既存 ADR の参照のみ）

## 動作確認方法

- Files タブで `docs/architecture/*.md` の Mermaid 図が描画されることを確認する
- `docs/README.md` → `overview.md` → 各 L2 → L3 のリンクを辿れることを確認する

## 備考

- 各 L2 文書はサブエージェントでコードと突合し、差異を反映済み（エンドポイント・設定既定値・ステータスコード・realm 設定・イベント記録契機・レート制限方式など）
- 読者テスト（コード未読で 6 文書だけを読ませる）で挙がった文書間の矛盾 5 件（公開閲覧ルーターの所在、`download` の記録タイミング、JWT 検証項目、実行履歴の並び順、`latest` の位置づけ）を解消済み
- 読者テストで残った要望のうち、正規化規則・JSON 構造・3 区分の詳細は L3（`data-model/idp-runs.md`、`viewer/result-panel.md`）へ委譲済みで、L2 には書かない


---

# Files changed

## added

- `docs/architecture/api.md`
- `docs/architecture/auth.md`
- `docs/architecture/view-url.md`
- `docs/architecture/viewer.md`

## modified

- `CLAUDE.md`
- `docs/api/README.md`
- `docs/architecture/README.md`
- `docs/architecture/overview.md`
- `docs/azure/README.md`
- `docs/develop-guide/auth.md`
- `keycloak/README.md`
