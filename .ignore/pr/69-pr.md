title:	chore: api/app配下のレイヤー型ディレクトリ再構成とapi/README.md作成（#68）
state:	MERGED
author:	shiro-ino (しろいの)
labels:	
assignees:	
reviewers:	
projects:	
milestone:	
number:	69
url:	https://github.com/shiro-ino/factone-idp-viewer/pull/69
additions:	112
deletions:	48
auto-merge:	disabled
--
# 概要

api/app 配下のフラットなモジュール構成を FastAPI 公式「Bigger Applications」準拠のレイヤー型構造（core/ routers/ services/）に再編し、api/README.md に各ディレクトリの責務と新規モジュール配置指針を記載しました。

## 関連Issue

Closes #68

## 変更内容

- git mv + import 修正のみ（ロジック変更なし）
  - `core/`: config, auth, db, static_serving
  - `routers/`: documents, idp_runs, view_urls
  - `services/`: blob, di, idp_extraction, pdf_render
  - `main.py` / `models.py` は `app/` 直下に維持
- `migrations/env.py` の `app.config` 参照を `app.core.config` へ修正
- `core/config.py` の `.env` アンカーを階層変更に合わせ `parents[2]` → `parents[3]` へ修正
- `api/README.md` 新規作成（ディレクトリ構成・責務・配置指針）

## 確認事項

### 共通

- [x] ブランチ名が規約に従っている（`chore/68-restructure-api-app`）
- [x] pre-commit のチェックがすべて通過している
- [x] 影響する設計ドキュメント（`docs/` 配下）を更新した、または更新不要である

### テスト

- [x] `uv run pytest` をローカルで実行し、すべて通過している（81 passed）
- [x] 重点テスト領域に変更がある場合、対応するテストを追加・更新した（ロジック変更なしのため追加なし）

### API契約

- [x] Pydanticモデルに変更なし（pre-commit の差分チェック通過）
- [x] 外部公開IFに影響なし

### アーキテクチャ

- [x] ディレクトリ再編は実装配置の整理でありADR対象の意思決定を含まない

## 動作確認方法

```
cd api
uv run pytest
uv run uvicorn app.main:create_app --factory
```

## 備考

diff は「ファイル移動＋import 文修正＋ `__init__.py` 追加＋README」のみで構成されています。唯一のコード修正は config.py のリポジトリルート算出（`parents[3]`）で、ファイル階層が1段深くなったことへの追随です。
