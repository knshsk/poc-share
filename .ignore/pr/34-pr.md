title:	docs: 決定済み事項のADR文書化とREADMEの現状整合（#33）
state:	MERGED
author:	shiro-ino (しろいの)
labels:	
assignees:	
reviewers:	
projects:	
milestone:	
number:	34
url:	https://github.com/shiro-ino/factone-idp-viewer/pull/34
additions:	274
deletions:	8
auto-merge:	disabled
--
# 概要

方針決定済み事項のADR 9本を追加し、各READMEの現状との齟齬を修正する。

## 関連Issue

Closes #33

## 変更内容

- ADR 0003〜0011を追加（テンプレート4節構成、status: accepted）
  - 0003 業務キー/文書キー分離（主参照キー=文書ID）
  - 0004 外部システム境界のアダプタ化
  - 0005 追記型ログ+生OCR/正規化値/確信度の分離
  - 0006 Keycloak採用+Container Apps相乗り配置
  - 0007 ネットワーク隔離なし・サービス側制御
  - 0008 フロント配信Container Apps同居
  - 0009 ブラウザ側認証SPA+OIDC/PKCE（BFFなし）
  - 0010 Blobアクセス API完全プロキシ
  - 0011 閲覧URL不透明トークン+DB管理
- README修正（現状との齟齬）
  - `keycloak/README.md`: 未コミット資料への参照ID除去、Azure投入方式を実装済みの実態（イメージ焼き込み）へ更新
  - `docs/api/README.md`・`docs/architecture/README.md`: 未コミット資料への参照ID除去
- 付随整理
  - ADR-0006のシークレット注入記述を実装（起動後`kcadm`スクリプト設定）に合わせて修正
  - ADR-0002・`docs/develop-guide/deploy.md`の参照表記整理

## 確認事項

### 共通

- [x] ブランチ名が規約に従っている（`feature/<issue番号>-<簡易説明>` または `fix/<issue番号>`）
- [x] pre-commit のチェックがすべて通過している
- [x] 影響する設計ドキュメント（`docs/` 配下）を更新した、または更新不要である

### テスト

- [x] `uv run pytest` / `npm run test` をローカルで実行し、すべて通過している（コード変更なしのため対象外）
- [x] 重点テスト領域（閲覧トークン、イベント記録、DI応答パース、座標変換、異常系）に変更がある場合、対応するテストを追加・更新した（該当なし）

### API契約

- [x] Pydanticモデルに変更がある場合、TS型を再生成し差分をコミットした（該当なし。pre-commit の差分チェックは通過）
- [x] 外部公開契約（Power Automate向けIF）に影響する場合、`docs/api` のOpenAPIエクスポートを更新した（該当なし）

### アーキテクチャ

- [x] アーキテクチャ上の意思決定を含む場合、ADR（`docs/adr/`）を作成・更新した

## 動作確認方法

なし（ドキュメントのみ）

## 備考

ADR本文の決定・却下理由は設計時のブレインストーミング資料から転記し、実装済みコードと突合済み。突合で見つかった記述齟齬（0008の配信実装方式、0006のシークレット注入方式）は実態に合わせている。

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Comments

