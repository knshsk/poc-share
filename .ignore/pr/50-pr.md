title:	chore: GiHubテンプレートの整備
state:	MERGED
author:	shiro-ino (しろいの)
labels:	
assignees:	
reviewers:	
projects:	
milestone:	
number:	50
url:	https://github.com/shiro-ino/factone-idp-viewer/pull/50
additions:	25
deletions:	22
auto-merge:	disabled
--
# 概要

GitHubテンプレート（Issue3種、Pull Request）の記述の微修正

## 関連Issue

Closes #49 

## 変更内容

- `bug.yml` `chore.yml` `feature.yml`
  - 対象コンポーネント（複数選択可）を修正
  - 要件IDや文書IDなど、リポジトリ外の資料に関する記述を削除
- `PULL_REQUEST_TEMPLATE.md`: `外部公開契約（Power Automate向けIF）` を `外部公開IF` という表現に変更

## 確認事項

### 共通

- [x] ブランチ名が規約に従っている（`feature/<issue番号>-<簡易説明>` / `fix/<issue番号>` / `chore/<issue番号>-<簡易説明>`）
- [x] pre-commit のチェックがすべて通過している（ソースコードの変更がないためスキップ）
- [x] 影響する設計ドキュメント（`docs/` 配下）を更新した、または更新不要である（更新不要）

### テスト

- [x] `uv run pytest` / `npm run test` をローカルで実行し、すべて通過している（ソースコードの変更がないためスキップ）
- [x] 重点テスト領域（閲覧トークン、イベント記録、DI応答パース、座標変換、異常系）に変更がある場合、対応するテストを追加・更新した（ソースコードの変更がないためスキップ）

### API契約

- [x] Pydanticモデルに変更がある場合、TS型を再生成し差分をコミットした（pre-commit の差分チェック通過）（ソースコードの変更がないためスキップ）
- [x] 外部公開契約（Power Automate向けIF）に影響する場合、`docs/api` のOpenAPIエクスポートを更新した（ソースコードの変更がないためスキップ）

### アーキテクチャ

- [x] アーキテクチャ上の意思決定を含む場合、ADR（`docs/adr/`）を作成・更新した（該当なし）

## 動作確認方法

なし

## 備考

なし


## Comments

