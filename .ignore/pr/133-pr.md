title:	chore: docs/viewer/display.md の閲覧イベント記録に関する古い記述を削除する（#126）
state:	MERGED
labels:	
assignees:	
reviewers:	
projects:	
milestone:	
number:	133
additions:	0
deletions:	1
auto-merge:	disabled
--
# 概要

`docs/viewer/display.md` の「実装ノート」に残っていた、閲覧・DLイベント記録を「後続で追加する」とする古い記述を削除する。閲覧イベント記録は ADR-0013 / ADR-0015 で実装済みで、同文書 10 行目の `page_view` の説明と矛盾していた。

## 関連Issue

Closes #126

## 変更内容

- `docs/viewer/display.md` の実装ノートから「閲覧・DLイベント記録は本方式の配信経路（APIプロキシ）上に後続で追加する」の 1 行を削除
- 同節の残り（オンデマンド変換）は変更なし

## 確認事項

### 共通

- [x] ブランチ名が規約に従っている（`feature/<issue番号>-<簡易説明>` / `fix/<issue番号>` / `chore/<issue番号>-<簡易説明>`）
- [x] pre-commit のチェックがすべて通過している
- [x] 影響する設計ドキュメント（`docs/` 配下）を更新した、または更新不要である

### テスト

- [x] `uv run pytest` / `npm run test` をローカルで実行し、すべて通過している（ドキュメントのみの変更のため対象外）
- [x] 重点テスト領域（閲覧トークン、イベント記録、DI応答パース、座標変換、異常系）に変更がある場合、対応するテストを追加・更新した（該当なし）

### API契約

- [x] Pydanticモデルに変更がある場合、TS型を再生成し差分をコミットした（該当なし）
- [x] 外部公開IFに影響する場合、`docs/api` のOpenAPIエクスポートを更新した（該当なし）

### アーキテクチャ

- [x] アーキテクチャ上の意思決定を含む場合、ADR（`docs/adr/`）を作成・更新した（該当なし）

## 動作確認方法

なし（ドキュメントのみ）

## 備考

`docs/viewer/` 配下に同種の古い記述が他に無いことを grep で確認済み。

---

# Files changed

## modified

- `docs/viewer/display.md`
