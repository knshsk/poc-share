title:	chore: docs/requirements新設とIDP/Viewer機能要件資料の格納（#57）
state:	MERGED
author:	shiro-ino (しろいの)
labels:	
assignees:	
reviewers:	
projects:	
milestone:	
number:	58
url:	https://github.com/shiro-ino/factone-idp-viewer/pull/58
additions:	169
deletions:	0
auto-merge:	disabled
--
# 概要

`docs/business/` の業務ドキュメント（operation-flow.md レーン定義・各工程、out-of-scope.md）からIDP/Viewerに求められる機能要件を導出し、`docs/requirements/` を新設して格納する。

## 関連Issue

Closes #57

## 変更内容

- `docs/requirements/README.md` を新規作成（目的・対象・出所・IDP/Viewer共通事項・記述方針）
- `docs/requirements/viewer.md` を新規作成（Viewer機能要件 VW-01〜VW-07。出所工程・関連OUT-xx・ADR-0011/0012への参照付き）
- `docs/requirements/idp.md` を新規作成（IDP機能要件 IDP-01〜IDP-04。出所工程・関連OUT-xxへの参照付き）
- `docs/README.md` のディレクトリ一覧に `requirements/` を追記

## 確認事項

### 共通

- [x] ブランチ名が規約に従っている（`feature/<issue番号>-<簡易説明>` / `fix/<issue番号>` / `chore/<issue番号>-<簡易説明>`）
- [x] pre-commit のチェックがすべて通過している
- [x] 影響する設計ドキュメント（`docs/` 配下）を更新した、または更新不要である

### テスト

- [x] `uv run pytest` / `npm run test` をローカルで実行し、すべて通過している（ドキュメントのみの変更でコード変更なし）
- [x] 重点テスト領域（閲覧トークン、イベント記録、DI応答パース、座標変換、異常系）に変更がある場合、対応するテストを追加・更新した（該当なし）

### API契約

- [x] Pydanticモデルに変更がある場合、TS型を再生成し差分をコミットした（該当なし）
- [x] 外部公開IFに影響する場合、`docs/api` のOpenAPIエクスポートを更新した（該当なし）

### アーキテクチャ

- [x] アーキテクチャ上の意思決定を含む場合、ADR（`docs/adr/`）を作成・更新した（該当なし。既存ADR-0011/0012の参照のみ）

## 動作確認方法

なし（ドキュメントのみ）

## 備考

要件と業務ドキュメント間の責務境界（採番＝Power Automate、送達＝CRM等）は operation-flow.md のレーン定義（非責務欄）に準拠。

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Comments

