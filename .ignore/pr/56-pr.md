title:	chore: CLAUDE.mdにブランチ運用・開発フローのルールを追記（#55）
state:	MERGED
author:	shiro-ino (しろいの)
labels:	
assignees:	
reviewers:	
projects:	
milestone:	
number:	56
url:	https://github.com/shiro-ino/factone-idp-viewer/pull/56
additions:	31
deletions:	1
auto-merge:	disabled
--
# 概要

ブランチ運用・開発フローのルールをCLAUDE.mdへ明文化する。クローンした他メンバーの環境でもエージェントがルールを参照できるようにする。

## 関連Issue

Closes #55

## 変更内容

- CLAUDE.mdに「開発プロセス」節を追記
  - ブランチ運用（GitHub Flow、main直接push禁止・PR必須、squash mergeのみ）
  - ブランチ名規則（feature/fix/chore）と区分基準
  - 開発フロー（Issue起票→ブランチ作成→実装→PR→レビュー→マージ）
  - 完了条件（品質ゲート通過、重点領域のテスト添付、ADR対象決定時のADR起票）

## 確認事項

### 共通

- [x] ブランチ名が規約に従っている（`feature/<issue番号>-<簡易説明>` / `fix/<issue番号>` / `chore/<issue番号>-<簡易説明>`）
- [x] pre-commit のチェックがすべて通過している
- [x] 影響する設計ドキュメント（`docs/` 配下）を更新した、または更新不要である

### テスト

- [x] `uv run pytest` / `npm run test` をローカルで実行し、すべて通過している（コード変更なしのため対象外）
- [x] 重点テスト領域（閲覧トークン、イベント記録、DI応答パース、座標変換、異常系）に変更がある場合、対応するテストを追加・更新した（該当なし）

### API契約

- [x] Pydanticモデルに変更がある場合、TS型を再生成し差分をコミットした（該当なし）
- [x] 外部公開IFに影響する場合、`docs/api` のOpenAPIエクスポートを更新した（該当なし）

### アーキテクチャ

- [x] アーキテクチャ上の意思決定を含む場合、ADR（`docs/adr/`）を作成・更新した（新規決定なし。既存決定の転記のみ）

## 動作確認方法

なし（ドキュメント変更のみ）

## 備考

なし

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Comments

