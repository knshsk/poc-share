title:	chore: 社内閲覧イベント非履歴化の確定化（ADR-0012追加・view-events更新）（#51）
state:	MERGED
author:	shiro-ino (しろいの)
labels:	
assignees:	
reviewers:	
projects:	
milestone:	
number:	52
url:	https://github.com/shiro-ino/factone-idp-viewer/pull/52
additions:	25
deletions:	1
auto-merge:	disabled
--
# 概要

社内（認証済み）閲覧イベントを履歴化しない決定を確定として反映する。コード変更なし（現行実装は既に非記録であり挙動不変）。

## 関連Issue

Closes #51

## 変更内容

- ADR-0012「社内閲覧イベントを履歴化しない」を追加（accepted）
- `docs/data-model/view-events.md` の社内表示非記録を仮置きから確定に更新（ADR-0012参照付き）

## 確認事項

### 共通

- [x] ブランチ名が規約に従っている（`feature/<issue番号>-<簡易説明>` / `fix/<issue番号>` / `chore/<issue番号>-<簡易説明>`）
- [x] pre-commit のチェックがすべて通過している
- [x] 影響する設計ドキュメント（`docs/` 配下）を更新した、または更新不要である

### テスト

- [x] `uv run pytest` / `npm run test` をローカルで実行し、すべて通過している（コード変更なしのため対象外）
- [x] 重点テスト領域（閲覧トークン、イベント記録、DI応答パース、座標変換、異常系）に変更がある場合、対応するテストを追加・更新した（該当なし）

### API契約

- [x] Pydanticモデルに変更がある場合、TS型を再生成し差分をコミットした（該当なし）
- [x] 外部公開IFに影響する場合、`docs/api` のOpenAPIエクスポートを更新した（該当なし）

### アーキテクチャ

- [x] アーキテクチャ上の意思決定を含む場合、ADR（`docs/adr/`）を作成・更新した

## 動作確認方法

なし（ドキュメントのみ）

## 備考

なし

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Comments

